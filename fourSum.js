function fourSum (nums, target){
    // sort the array
    let sorted = nums.sort((a,b) => a - b);
    const result = [] // result to be stored in an array;
    const n = sorted.length;

    // iterate over first two element
    for (let i = 0; i < n  - 3; i ++){
        
    }

}

console.log(fourSum([1,2,3,4], 6));